from flask_sqlalchemy import SQLAlchemy

db=SQLAlchemy()

































# stores = {}
# items = {} #We adopted this change so that we can reference items and stores through an id (ex: 1 and)